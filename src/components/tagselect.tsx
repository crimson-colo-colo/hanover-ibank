function TagSelector() {
    return (
        <div>
            <label htmlFor="tagselect">Select Material Type</label>
            <select id="tagselect" name="options">
                <option value="1">Reference Material</option>
                <option value="2">Workflow Material</option>
            </select>
        </div>
    )
}
